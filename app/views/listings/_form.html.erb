<%= form_with(model: listing, local: true) do |form| %>
  <% if listing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(listing.errors.count, "error") %> prohibited this listing from being saved:</h2>

      <ul>
        <% listing.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

    <div>
      <%= form.label :photo %> <%= form.file_field :photo, accept:'image/png,image/gif,image/jpeg,image/jpg' %>
    </div>

    <div>
        <%= form.label :title %>
        <%= form.text_field :title %>
    </div>
    <div>
        <%= form.label :description %>
        <%= form.text_area :description %>
    </div>

    <%# drop down code %>
    <div>
        <%= form.label :category %>
        <%= form.collection_select :catgeory, @categories, :id, :name %>
    </div>

    <%# drop down code %>
    <div>
        <%= form.label :price_type %>
        <%= form.collection_select :price_type, @price_type, :id, :name %>
    </div>

    <div>
        <%= form.label :price %>
        <%= form.number_field :price, min: 0 %>
    </div>

    <%# <div> %>
        <%# <%= form.label :location %> %>
        <%# <%= form.text_field :location %> %>
    <%# </div> %>

    <%# timestamp here? %>
    <%# <div> %>
        <%# <%= form.label :created_at %> %>
        <%# <%= form.number_field :created_at %> %>
    <%# </div> %>
 

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

